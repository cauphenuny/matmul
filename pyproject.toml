[project]
name = "matmul"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "numpy>=2.2.6",
    "pybind11>=2.13.6",
]

[dependency-groups]
dev = ["pytest>=8.3.5"]
